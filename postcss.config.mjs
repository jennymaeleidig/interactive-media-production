// Tailwind v4 reads its content from the project automatically and its theme
// from the `@theme` block below (CSS-first, no config file). This stylesheet is
// the one authoring file for the piece's surfaces.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
