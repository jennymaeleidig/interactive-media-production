# 21: scroll choreography and modals

**What to build:** The `/safe-cities` page's scroll-driven behavior and its
"see it in action" modal, which the Capture strips (the live page drives them
with inline GSAP/ScrollTrigger scripts and Webflow's IX2 data). A vanilla
runtime — no GSAP, no CDN, so the zero-outbound invariant holds — restores:

- `[animate="scrub-word"]` headings: each word runs the captured color
  sequence dark → green → near-white as the heading scrolls in, reversing when
  it scrolls back out.
- `.line-label` / `.line-label--marker`: the badge pill scales `0 → 1` and its
  marker slides up `170% → 0` on scroll-in.
- `[data-scroll-video]`: play at `top 45%`, pause at `bottom 20%`.
- `#stickme`: the sticky "see it in action" button pins to the viewport bottom
  from the moment its section is on screen until the page scrolls back above
  it — a page-wide control, not one bounded by its parent's box.
- `#main-progress` / `.main-line`: the page's SVG route draws as it scrolls.
- `.c-modal` dialogs: `data-c-modal-open="<id>"` opens
  `dialog[data-c-modal="<id>"]`; `[c-modal-close]`, backdrop, and Escape close
  it. Opening runs the modal's own route draw (`#route-progress` scrubbed on
  `[c-modal-scroll]`), and each `.marker[data-stop]` is placed along the path
  opposite its timeline event and pops in as that event scrolls into view.

Every behavior keys on captured attributes/classes (no per-page logic), so the
layer covers any page carrying them. The build normalizes the captured
from-states to their end-states first, so a JavaScript-disabled or
reduced-motion page renders the settled layout — which also folds the
`/safe-cities` scrub-word white override into the animation itself (the
dark-subhead contrast fix stays).

**Blocked by:** 12 (the human review that found it).

**Status:** resolved

- [x] `[animate="scrub-word"]` words animate dark → green → light on scroll-in and reverse on scroll-back.
- [x] `.line-label` and its marker reveal on scroll-in; they are visible (end-state) with JavaScript disabled.
- [x] `[data-scroll-video]` videos play while in view and pause out of view.
- [x] `#stickme` stays in its captured in-flow position under the intro paragraph — never pinned (review decision, addendum 3).
- [x] `#main-progress` draws with page scroll.
- [x] `data-c-modal-open` opens its `<dialog>`; close/backdrop/Escape close it; focus and body scroll are restored.
- [x] The modal's `#route-progress` draws with the modal's own scroll, and `.marker[data-stop]` markers are placed and pop in.
- [x] Reduced motion and JavaScript-disabled pages render the static end-state (reduced motion keeps function: the modal still opens).
- [x] No GSAP, no remote script, no network; generated by `pipeline/scroll-runtime.js` + `pipeline/scroll.css` as a marked inline layer.

## Comments

**Implemented.** Build pass 13 (`scrollPass`) normalizes the captured scroll
from-states to their end-states and injects `pipeline/scroll.css` +
`pipeline/scroll-runtime.js` inline, marked `data-flock-parody="scroll"` — the
same shape as the motion/interactions layers. The runtime is vanilla JS (no
GSAP, no CDN, so the zero-outbound invariant holds) and keys only on captured
attributes/classes. On `/safe-cities` the build log reads: `line-label scale
(0→1)` ×3, `line-label marker slide (170%→0)` ×3, `scrub-word from-color` ×3,
`main-progress draw (undrawn→drawn)` ×1, `modal panel slide` ×1,
`section zoom settle (0.9→1)` ×1.

- **The captured behavior**, from the live page's own inline scripts and the
  Webflow IX2 data in the site bundle: scrub-word colors (IX2 `t-67b5deff`:
  dark `hsla(100,12.68%,13.92%)` → green `hsla(106.9,59.78%,63.92%)` → near-white
  `hsla(105,11.11%,92.94%)`, .05s/word stagger); `.line-label` scale and marker
  slide (IX2 `t-4e5bbe4b`); `[data-scroll-video]` ScrollTrigger play/pause;
  `#stickme` scroll toggle; `#main-progress`/`#route-progress` scrubbed SVG
  draws; the `dialog.c-modal` system and the modal's marker placement.
- **Reduced motion** skips only the animation: the modal, the sticky button,
  and the marker placement still work (ticket 05's function-never-blocked
  ruling). `test/scroll.seam.test.ts` covers both branches (8 tests).
- **Gates:** `npm run pipeline` 1,181 served (all pages carry the layer, audit
  clean, 0 executable capture-derived scripts) · `npm run build` and
  `npm run routes` green (1,290 routes, 1,181 byte-identical, 2,960 assets) ·
  `npm run typecheck` clean · 337 tests across 17 files.
- The `/safe-cities` scrub-word white override from ticket 12's legibility fix
  is retired — the heading's own light color is the normalized end-state and the
  runtime owns the colors; `LEGIBILITY_PATCHES` keeps only the dark-subhead
  rule.

**Addendum: the sticky button flickered (human review, 2026-09-12).** Two
faults in this layer, both now fixed:

- The stick decision measured the button's own rect. `.stick` makes the button
  `position: fixed`, which moves that rect into viewport coordinates, so the
  test flipped back on the very next scroll event — the class alternated
  between pinned and in-flow on every tick. It now measures the parent (which
  does not move when the button does) and reads the button's offset inside it
  once, while it is still unstuck.
- `.bg-screen.scroller` carries the captured inline `transform: scale(0.9)` —
  the frozen from-state of the site's scroll zoom (live settles at scale 1). A
  non-`none` transform makes the section the containing block for
  `position: fixed` descendants, so the pinned button was placed at the
  section's bottom (~2,800px below the fold) instead of the viewport bottom.
  The build now drops that from-state (`section zoom settle (0.9→1)`), which
  also removes the 10%-smaller section and the seam it left at the page edges.
- `.stick--is-stuck` now keeps `.stick` applied: the captured CSS's
  `.stick{top:auto}` is what stops `.stick--is-stuck`'s `top:0` stretching the
  wrapper to the parent's height.

Verified with real wheel input on the served page: in flow at the top, pinned
(`bottom` = the viewport bottom) once the section reaches the fold,
`stick--is-stuck` at the section's end with the button riding the parent's
bottom, then back to pinned on scroll-up — with no class churn in between; plus
a `test/scroll.seam.test.ts` regression that re-stubs the button's own rect
while pinned. (The hand-off described in this paragraph was removed again after
the second human review — see the addendum below.)

### Addendum 2 (human review): the button "scrolled with the page"

The human review came back on the fixed build with "the button scrolls with the
page? its supposed to remain in place", and chose the page-wide option when
asked which behavior was wanted. Two things changed, both in the runtime:

- **The decision is now `parentRect.top < vh()`** — no button geometry at all
  (the natural offset/height reads are gone), so it stays monotone in scroll
  position and cannot oscillate, and the control pins the moment its section
  reaches the fold rather than when its own place crosses it. Pinning is
  deliberately one-way for the rest of the page: a control that hands back at
  its parent's end and rides the content away is what read as broken. The
  `.stick--is-stuck` hand-off state is gone.
- **The pin no longer trusts `position: fixed` to have worked.** Any ancestor
  that is a containing block for fixed boxes — a captured `transform`, a
  `filter`, a `will-change`, a `contain` — captures the button, and the
  captured stylesheet's own `#stickme.stick` rule can be overridden. Every tick
  re-measures where the button actually landed, and a pin that is not within
  2px of the viewport bottom is redone by the runtime in document coordinates
  (`position: absolute` + a `top` recomputed from the offset parent), cleared
  again when the control returns to the flow. `scroll.css` also carries the
  pinned state itself (`html.fpm-scroll #stickme.stick`), without
  `!important`, so that placement can win.

Verified with real wheel input on the served page with motion **on** (a
throwaway server that forces `prefers-reduced-motion: no-preference`, because
this session's headless Chrome otherwise takes the reduced path): `y=0` in flow
at `988` (its parent's top), `y=600` pinned (`574..760`, the viewport bottom,
before its own place had reached the fold), `y=2100` pinned, and `y=5200` —
1,619px past the parent's end and 2,615px past the section's end, with the page
ending at `6,097` — still pinned at `574..760` with no `stick--is-stuck` and no
inline fallback needed, then back to the flow at `y=0` on scroll-up. Zero
uncaught errors throughout; the same `y=5200` pin holds on the server with
reduced motion. `test/scroll.seam.test.ts` carries both cases: the page-wide
pin (its stubbed rect follows the class the way a browser honoring `fixed`
reports it) and the fallback (a rect that never reaches the viewport bottom →
the runtime places the button itself and refreshes `top` every tick).

### Addendum 3 (human review): the pin is removed — the button is static

The third review rejected the page-wide pin too: "the see it in action button is
supposed to stay here" — under the intro paragraph, at the top of
`#stickme-parent` — "not here", at the viewport bottom (two screenshots: the
wanted in-flow position, and the pinned position it must not take), and "its
static with scrolls".

The pin is gone from the build. Nothing in the runtime or `scroll.css` touches
`#stickme` any more; the button keeps the captured layout CSS
(`#stickme { position: absolute; top: 0 }` inside its relative parent), which is
straight below the intro paragraph.

Why this is the faithful reading, checked against the live page rather than
inferred:

- **The live page's own sticky script never attaches.** The inline
  `stick('#stickme', 'bottom')` call runs *before* the `#stickme` markup is
  parsed (`compareDocumentPosition` on the live DOM puts the script ahead of the
  button), so `document.querySelector('#stickme')` is null, `stick.parentNode`
  throws a `TypeError`, and the scroll listener is never wired. A live scroll
  sweep from `y=0` to `y=5371` never added `stick` to the element: its viewport
  top moved linearly from `965` to `-4383` and it simply scrolled out of view.
- **Even if the class were toggled, the pin could not work there.** The ancestor
  `<section class="... bg-screen scroller">` carries the captured inline
  `transform: scale(0.9,0.9)` (the site's IX2 zoom from-state), which is a
  containing block for `position: fixed`; with `stick` added by hand at
  `y≈1200` the wrapper's rect sat at viewport `2319..2505` — far below the fold.
  The build drops that transform as a separate normalization
  (`section zoom settle (0.9→1)`), which stays.
- **A bottom-pin could never produce the wanted frame.** The wanted screenshot
  has the button mid-viewport directly under the paragraph; any pin to the
  viewport bottom puts it at the bottom edge by construction.

Removed: the `#stickme` block in `pipeline/scroll-runtime.js` (class toggle from
`parentRect.top < vh()` plus the manual `position: absolute` placement fallback
for containing-block pages), the `html.fpm-scroll #stickme.stick` rule in
`pipeline/scroll.css`, and the unused `setClass` helper. The
`describe('#stickme sticky button')` seam in `test/scroll.seam.test.ts` is
replaced by `describe('#stickme stays in flow')`, which asserts the runtime never
adds `stick`/`stick--is-stuck` and never writes an inline `position`/`top`/
`bottom` — even with the parent's top already inside the viewport (the case that
used to pin). The stale pin references in `CODING_STANDARDS.md` and
`spec.md` are updated to match; the `.bg-screen.scroller` zoom normalization
stays, now documented as a fidelity normalization only.

Restoring a pin, if ever wanted, is a revert of this change plus the seam
regression test — the removals are purely subtractive.
