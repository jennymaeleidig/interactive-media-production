Status: resolved
Type: prototype
Blocked by: 08

## Question

The Recreation's pages are static frozen end-states (ratified, ticket 05) — motion is restored as a **cheap mimic layer**, not the original GSAP/Lenis runtime. Ticket 08's census names the site's distinct animation patterns; for each pattern family, decide which mimicry tier applies, and prove the feeling once on the homepage:

1. **CSS-only** — `scroll-behavior: smooth` for the Lenis feel; `@keyframes` + `animation-fill-mode: forwards` (or `animation-timeline: view()` for scroll reveals); `transition:` rules for hover tweens.
2. **Shared reveal snippet** — one ~30-line IntersectionObserver + `data-animate`-style annotations on the captured DOM + per-pattern CSS pairs authored from ticket 08's captured end-state values. Declarative annotation on top of the captured DOM, not per-page bespoke logic; `prefers-reduced-motion` honored by not annotating. Static-first: the captured end-state is the default, motion is enhancement.
3. **End-state only** — the line for anything scroll-scrubbed/pinned (ScrollTrigger timelines) the census flags as hard to fake cheaply: the page ships as captured.

Build the chosen layers in the same shape as the story-hook seam (shared snippet injected by the build pass, declarative per-element annotations), wire the Marketo form decision in as its first real case (static styled mock of the rendered end-state → existing mock route — ticket 05), walk it with the user, then write the spec wording: what motion the Recreation ships, how it's authored, and how it's regression-checked (extend the serving-gate diff if annotations change default rendering).

## Answer

Resolved 2026-09-10 — layer built inside the ticket-05 serving prototype (pass 4 + `pipeline/motion.css` / `motion-runtime.js`; full write-out: [prototype/snapshot-serving/MOTION.md](../prototype/snapshot-serving/MOTION.md)), walked by the user; tier table and interaction scope ratified in-session.

1. **Tier table (ratified)** — census found no scrubbed motion in the prototype scope, so tier 3 starts empty there: tier 1 CSS-only = smooth-scroll feel (`scroll-behavior: smooth`), the LPR `data-animation-css` keyframes (**free — captured CSS plays with no JS**), hover transitions (authored at build); tier 2 shared snippet = hero split (runtime re-fires `.is-visible`; **the captured transition CSS plays it verbatim**, 58ms/word stagger), scroll word-splits (incl. the LPR masked `.split-word` variant — same captured from-inline), fade-in/fade-in-2/clip-in/image-clip (IO one-shot → `.fpm-in`); click-driven families = a delegated-interaction sibling (tabs/dropdowns/accordion/Swiper — **all four in**, per user; LPR accordion function-only, no height tween). `prefers-reduced-motion` → no class, no motion; no JS → same. Static-first by construction: the build pass normalizes every captured from-state to end-state before injecting the layer.
2. **Architecture** — mirrors the story-hook seam: pass 4 normalizes from-states (logged per page in `build-log.json` under `motion`; homepage 38 words + 3 fade-in-2 + 1 image-clip; LPR 14 + 16; press-center 1) + annotates split words with `--fpm-i`, then injects the CSS+runtime pair inline. No new per-page logic anywhere.
3. **Regression** — serving gate stays **0 px at 1440/768/390** with the layer aboard; gate shots now `--force-prefers-reduced-motion` (compositor transitions race virtual-time — real 390-wide flake, fixed by pinning the static contract the runtime itself honors). Strip report **pixel-identical to the ticket-05 baseline** (same diff PNG): the motion layer adds zero new visual deltas; the human side-by-side at the phase gate still covers runtime feel.
4. **Marketo static styled mock is FREE** (corrects ticket 05's "frozen pre-JS worst case"): all five captured mkto CSS blocks are live in the served page — `sf-hidden` on a `<style>` is cosmetic, and the site override's `width:100%!important` beats the frozen 2531px inline. `/book-a-demo` renders fully styled (`shots/t10_bookademo_tall.png`); the mock decision reduces to captured-form-as-is + the pass-3 action injection.
5. **IX2 generic sweep (user deferred to me): include it at build time.** Beyond the attribute-targeted normalization, the build pass sweeps any inline `opacity:0` IX2 from-state → end-state + tags `data-fpm-reveal` for the IO layer (the one added-attribute seam, same as story-hooks). Rationale learned this session: attribute-targeted sweeps miss systems nobody named (see 6). Review surface: build log + strip report human review, which surfaces every region normalization touched.
6. **Live-check finding (user approved the investigation): the census's "zero scrubbed anywhere" is wrong in a small, bounded way.** Live-config analysis (fetched the live `/flock-ecosystem` HTML + its 22 JS bundles): the `data-animation-gsap` timelines are `scrub:null` (one-shot — the mimic's model is exact), and `home4_scroll_*` is a **click-tab system** (`data-tabs`), not scroll-linked — census worry resolved. BUT the site bundle carries real `scrub:.8` timelines targeting a selector set (`.dfr_cards_wr`, `.drone-img`, `[animate=scrub-word]`, `.line-label`) whose elements exist on **exactly 2 pages**: `/safe-cities` and `/products/flock-dfr` (the 5 blog `line-label` grep hits are CSS/data false positives). Tier 3 applies there: **ships as captured**. Caveat for /to-spec: `/safe-cities`' scrub-driven labels are frozen at `scale(0,0)` — invisible in the served page, same accepted-dead-path status as `/chilipiper-2`; also a ready-made story-hook target for the parody phase.
7. **Spec wording (what /to-spec inherits)**: the Recreation ships static end-states normalized by the build pass, motion as the injected mimic layer per the tier table, interactions per the delegated-click sibling; gate = serving diff at 0 px under forced reduced-motion; feel = human side-by-side at phase gates; IX2 coverage via the generic sweep with log-reviewed deltas.

Prototype: `.scratch/flock-parody/prototype/snapshot-serving/` (throwaway, runnable — `MOTION.md` has run instructions).

## Comments

**2026-09-10 (working session):** Motion mimic layer built inside the ticket-05 serving prototype (same pipeline, new pass 4 + `motion.css`/`motion-runtime.js`; write-up: `prototype/snapshot-serving/MOTION.md`). Verified end-to-end: from-state normalization logged per page (homepage 38 words + 3 fade-in-2 + 1 image-clip; LPR 14 masked split-words + 16 fade-ins; press-center 1 bare fade), serving gate **0 px at all three viewports** with the layer aboard (gate shots now force `prefers-reduced-motion` — compositor transitions race virtual-time otherwise; the runtime honors it by design), strip report **pixel-identical to the ticket-05 baseline**. Bonus finding: the Marketo static mock is **free** — all five captured mkto CSS blocks are live in the served page (`sf-hidden` on `<style>` is cosmetic; the site override's `width:100%!important` beats the frozen 2531px inline), `/book-a-demo` renders the form fully styled. Prototype built and self-walked via Docker screenshots; **ticket stays claimed** — prototype tickets resolve through the user's walkthrough (tier-table ratification + interaction scope: tabs proven, dropdowns/accordion/swiper coded-but-unproven, IX2 generic sweep deferred to spec, `home4_scroll_*` live-check optional).
