node_modules/
.pi/npm/
.next/
.sandbox/
.DS_Store

# Build outputs — `.tmp/` is scratch. `served/` is deliberately NOT ignored:
# it is the Recreation itself, and nothing can rebuild it (its captures are gone).
.tmp/
*.tsbuildinfo
