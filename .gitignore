node_modules/
.pi/npm/
.next/
.sandbox/
.DS_Store

# Build outputs
served/
.tmp/
*.tsbuildinfo

# Capture runs: heavy SingleFile HTML stays out of git (reproducible via the
# docs/capture-refresh-runbook.md procedure); capture lists, status/manifest
# CSVs, and reports stay tracked, as do dated inventories
# (research/inventory/*.csv, *-diff.md, *-diff.csv, *-recapture.txt).
.scratch/flock-parody/research/flocksafety/**/*.html
# exception: the committed 11.9 MB homepage probe ticket 02 analyses (wayfinding
# evidence); every dated run folder below is ignored
!.scratch/flock-parody/research/flocksafety/index.html
.scratch/flock-parody/research/flocksafety/**/walk.log
