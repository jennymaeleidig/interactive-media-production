node_modules/
.pi/
.next/
.sandbox/
.DS_Store
.scratch/
.agents/
skills-lock.json

# Build outputs — none of these is committed. The chat's generated program and
# runtime (`scripts/chat-program.json`, `scripts/chat-runtime.js`) are the bytes
# the piece ships, so they are tracked deliberately; `npm run chat:check` is what
# keeps them tied to their sources.
out/
.tmp/
*.tsbuildinfo
